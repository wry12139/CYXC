# AI 问答(术语兜底型)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给财小白加一层 AI 名词科普兜底——术语搜索本地词库未命中时,已登录用户可点"问问 AI"调独立 AI 服务(8092)获取白话科普。

**Architecture:** 新增独立 Python 标准库 HTTP 服务(端口 8092),与现有 8091 用户后端进程隔离但共享同一 `finrookie.db`(只读复用 `auth.lookup_session` 验 token)。缓存表匿名不含 user_id。前端在现有术语搜索空态上加一层 AI 兜底按钮。

**Tech Stack:** Python 3 标准库(http.server / urllib / sqlite3 / ssl / hashlib),前端原生 ES module + Alpine.js,无 pip 无构建。

## Global Constraints

- **纯 Python 标准库,零 pip 依赖**(延续服务器"无 node/nginx/flask"哲学)。
- **TLS 必须验证**:`ssl.create_default_context()`,绝不 `CERT_NONE`(AI 无审核直出给用户,须防中间人)。
- **AI Key 绝不落前端**:仅后端读 `~/finrookie-secret/ai.env`(`FR_AI_KEY/FR_AI_BASE/FR_AI_MODEL`)。
- **模型固定** `claude-haiku-4-5-20251001`(与 pipeline 一致,存于 ai.env 的 FR_AI_MODEL)。
- **身份唯一来源是 `Authorization: Bearer <token>`**,从不信任前端传的 user_id。
- **缓存表 `ai_cache` 不含 user_id**:匿名共享,任何登录用户命中同一问题秒回。
- **无任何"列出提问"接口**:`/api/ask` 只吃单问题、只吐单答案。
- **合规中等尺度**:可讲策略概念,绝不推荐具体标的、不预测涨跌。入口拒答 + 出口过滤双保险。
- **前端裸读守卫**:Alpine x-show 内表达式仍求值,`aiAnswer` 初始 null,所有 `aiAnswer.xxx` 加 `aiAnswer &&` 守卫。
- **前端 API_BASE** = `http://10.159.3.80:8092`。
- 现有后端文件 `db.py / auth.py / server.py` **不修改**。

---

### Task 1: 合规模块 compliance.py

**Files:**
- Create: `backend/compliance.py`
- Test: `backend/tests/test_ai.py`(本任务新建,后续任务续加)

**Interfaces:**
- Produces:
  - `BANNED: list[str]` — 违规词表(从 pipeline/ai_rewrite.py 复制,保持一致)
  - `has_banned(text: str) -> list[str]` — 返回命中的违规词列表,空列表表示干净
  - `SAFE_FALLBACK: str` — 命中违规时的固定安全话术
  - `input_blocked(question: str) -> bool` — 入口判断:提问是否涉及具体买卖/荐股意图

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_ai.py
import os, sys, unittest
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import compliance

class TestCompliance(unittest.TestCase):
    def test_has_banned_detects_violation(self):
        self.assertTrue(compliance.has_banned("这只股票必涨"))
    def test_has_banned_clean_text(self):
        self.assertEqual(compliance.has_banned("ETF 是一种基金"), [])
    def test_input_blocked_on_buy_intent(self):
        self.assertTrue(compliance.input_blocked("我该不该买入贵州茅台"))
    def test_input_allowed_concept(self):
        self.assertFalse(compliance.input_blocked("什么是ETF"))
    def test_safe_fallback_exists(self):
        self.assertIn("概念", compliance.SAFE_FALLBACK)

if __name__ == '__main__':
    unittest.main()
```

- [ ] **Step 2: 运行验证失败**

Run: `cd backend && python3 -m pytest tests/test_ai.py -v`(或 `python3 -m unittest tests.test_ai -v`)
Expected: FAIL —— `ModuleNotFoundError: No module named 'compliance'`

- [ ] **Step 3: 写实现**

```python
# backend/compliance.py
# 违规词表与 pipeline/ai_rewrite.py 的 BANNED 保持一致(手动同步的两份副本)
BANNED = [
    "买入", "卖出", "加仓", "减仓", "抄底", "追高", "建仓", "清仓", "满仓", "梭哈",
    "买它", "值得买入", "可买", "可入", "上车",
    "推荐股票", "荐股", "目标价", "牛股", "翻倍", "黑马", "潜力股", "首选", "金股",
    "必涨", "必跌", "涨停板", "稳赚", "包赚", "一定会涨", "一定会跌", "稳赢", "保本高息",
    "预测", "内幕", "可关注", "逢低布局", "逢低吸纳", "看多", "看空", "上行空间",
    "建议持有", "短线机会", "重点关注个股", "低吸", "高抛",
]

SAFE_FALLBACK = "我只能帮你理解概念,不能给出具体买卖或涨跌建议哦~"

# 入口意图词:提问里出现这些即拒答(用户想要买卖/荐股决策)
_INTENT = ["该不该买", "能买吗", "可以买", "值得买", "买不买", "推荐", "买哪",
           "该买", "该卖", "能涨", "会涨", "会跌", "涨到", "跌到", "目标价"]

def has_banned(text):
    return [w for w in BANNED if w in text]

def input_blocked(question):
    return any(w in question for w in _INTENT)
```

- [ ] **Step 4: 运行验证通过**

Run: `cd backend && python3 -m unittest tests.test_ai -v`
Expected: PASS(5 条)

- [ ] **Step 5: 提交**

```bash
git add finrookie/backend/compliance.py finrookie/backend/tests/test_ai.py
git commit -m "feat(backend): add compliance module for AI Q&A input/output filtering"
```

---

### Task 2: AI 缓存表 + 数据访问 ai_cache.py

**Files:**
- Create: `backend/ai_cache.py`
- Test: `backend/tests/test_ai.py`(续加)

**Interfaces:**
- Consumes: `db.get_conn(db_path)`(现有)
- Produces:
  - `ensure_table(conn) -> None` — `CREATE TABLE IF NOT EXISTS ai_cache`,不含 user_id
  - `normalize(question: str) -> str` — 去空格 + 小写
  - `cache_key(question: str) -> str` — sha256(normalize(question)) 十六进制
  - `get_cached(conn, question: str) -> str | None` — 命中返 answer,否则 None
  - `put_cached(conn, question: str, answer: str) -> None` — 写入(存原始 question + answer)

- [ ] **Step 1: 写失败测试**

```python
# 追加到 backend/tests/test_ai.py
import sqlite3
import db as db_module
import ai_cache

class TestAiCache(unittest.TestCase):
    def setUp(self):
        self.conn = sqlite3.connect(':memory:')
        db_module_init = self.conn  # in-memory,直接建表
        ai_cache.ensure_table(self.conn)
    def tearDown(self):
        self.conn.close()
    def test_normalize_ignores_space_and_case(self):
        self.assertEqual(ai_cache.normalize("什么是 ETF"), ai_cache.normalize("什么是etf"))
    def test_cache_miss_returns_none(self):
        self.assertIsNone(ai_cache.get_cached(self.conn, "什么是可转债"))
    def test_put_then_get_hits(self):
        ai_cache.put_cached(self.conn, "什么是ETF", "ETF是一种基金")
        self.assertEqual(ai_cache.get_cached(self.conn, "什么是 ETF"), "ETF是一种基金")
    def test_table_has_no_user_id_column(self):
        cols = [r[1] for r in self.conn.execute("PRAGMA table_info(ai_cache)").fetchall()]
        self.assertNotIn("user_id", cols)
```

- [ ] **Step 2: 运行验证失败**

Run: `cd backend && python3 -m unittest tests.test_ai -v`
Expected: FAIL —— `No module named 'ai_cache'`

- [ ] **Step 3: 写实现**

```python
# backend/ai_cache.py
import hashlib
from datetime import datetime, timezone

def ensure_table(conn):
    conn.execute("""
    CREATE TABLE IF NOT EXISTS ai_cache (
        question_hash TEXT PRIMARY KEY,
        question      TEXT NOT NULL,
        answer        TEXT NOT NULL,
        created_at    TEXT NOT NULL
    )""")
    conn.commit()

def normalize(question):
    return ''.join(question.split()).lower()

def cache_key(question):
    return hashlib.sha256(normalize(question).encode('utf-8')).hexdigest()

def get_cached(conn, question):
    row = conn.execute(
        "SELECT answer FROM ai_cache WHERE question_hash=?",
        (cache_key(question),)).fetchone()
    return row[0] if row else None

def put_cached(conn, question, answer):
    conn.execute(
        "INSERT OR REPLACE INTO ai_cache (question_hash, question, answer, created_at) "
        "VALUES (?,?,?,?)",
        (cache_key(question), question, answer, datetime.now(timezone.utc).isoformat()))
    conn.commit()
```

- [ ] **Step 4: 运行验证通过**

Run: `cd backend && python3 -m unittest tests.test_ai -v`
Expected: PASS(累计 9 条)

- [ ] **Step 5: 提交**

```bash
git add finrookie/backend/ai_cache.py finrookie/backend/tests/test_ai.py
git commit -m "feat(backend): add anonymous ai_cache table and data access"
```

---

### Task 3: AI 调用客户端 ai_client.py

**Files:**
- Create: `backend/ai_client.py`
- Test: `backend/tests/test_ai.py`(续加,用 mock 不真调网络)

**Interfaces:**
- Produces:
  - `SYSTEM_PROMPT: str` — 中等尺度人设
  - `load_cfg() -> dict` — 读 ai.env,返 `{FR_AI_KEY, FR_AI_BASE, FR_AI_MODEL}`
  - `ask(question: str, cfg: dict) -> str` — 调中转站返答案文本,失败抛异常

- [ ] **Step 1: 写失败测试(mock urlopen)**

```python
# 追加到 backend/tests/test_ai.py
from unittest import mock
import ai_client

class TestAiClient(unittest.TestCase):
    def test_ask_parses_answer(self):
        fake = mock.Mock()
        fake.read.return_value = ('{"choices":[{"message":{"content":"ETF是一种基金"}}]}').encode('utf-8')
        cfg = {"FR_AI_KEY": "k", "FR_AI_BASE": "https://x", "FR_AI_MODEL": "claude-haiku-4-5-20251001"}
        with mock.patch('urllib.request.urlopen', return_value=fake):
            out = ai_client.ask("什么是ETF", cfg)
        self.assertEqual(out, "ETF是一种基金")
    def test_system_prompt_forbids_recommendation(self):
        self.assertIn("不", ai_client.SYSTEM_PROMPT)
        self.assertTrue("推荐" in ai_client.SYSTEM_PROMPT or "买卖" in ai_client.SYSTEM_PROMPT)
```

- [ ] **Step 2: 运行验证失败**

Run: `cd backend && python3 -m unittest tests.test_ai -v`
Expected: FAIL —— `No module named 'ai_client'`

- [ ] **Step 3: 写实现**

```python
# backend/ai_client.py
import os, ssl, json, urllib.request

SECRET = os.path.expanduser("~/finrookie-secret/ai.env")
_ctx = ssl.create_default_context()  # TLS 必须验证
_REQUIRED = ("FR_AI_KEY", "FR_AI_BASE", "FR_AI_MODEL")

SYSTEM_PROMPT = (
    "你是财小白的金融科普助手,面向零基础新手。只做名词解释和概念科普,"
    "可以讲清策略概念(如什么是定投),但绝不推荐具体股票/基金标的,"
    "绝不预测涨跌,绝不给买卖建议。回答简短白话,不超过150字。"
)

def load_cfg():
    cfg = {}
    with open(SECRET, encoding="utf-8") as f:
        for line in f:
            line = line.strip()
            if line and not line.startswith("#") and "=" in line:
                k, v = line.split("=", 1)
                cfg[k.strip()] = v.strip()
    missing = [k for k in _REQUIRED if not cfg.get(k)]
    if missing:
        raise RuntimeError(f"ai.env 缺少必填项: {missing}")
    return cfg

def ask(question, cfg):
    body = json.dumps({
        "model": cfg["FR_AI_MODEL"],
        "messages": [
            {"role": "system", "content": SYSTEM_PROMPT},
            {"role": "user", "content": question},
        ],
        "max_tokens": 400,
        "temperature": 0.3,
    }).encode("utf-8")
    req = urllib.request.Request(
        cfg["FR_AI_BASE"] + "/v1/chat/completions",
        data=body,
        headers={"Authorization": "Bearer " + cfg["FR_AI_KEY"],
                 "Content-Type": "application/json"})
    r = urllib.request.urlopen(req, timeout=20, context=_ctx)
    d = json.loads(r.read().decode("utf-8", "replace"))
    return d["choices"][0]["message"]["content"].strip()
```

- [ ] **Step 4: 运行验证通过**

Run: `cd backend && python3 -m unittest tests.test_ai -v`
Expected: PASS(累计 11 条)

- [ ] **Step 5: 提交**

```bash
git add finrookie/backend/ai_client.py finrookie/backend/tests/test_ai.py
git commit -m "feat(backend): add AI client with TLS-verified relay call and system prompt"
```

---

### Task 4: 独立 AI 服务 ai_server.py

**Files:**
- Create: `backend/ai_server.py`
- Test: `backend/tests/test_ai.py`(续加,端到端 mock ai_client.ask)

**Interfaces:**
- Consumes: `db.get_conn`、`auth.lookup_session`、`compliance.*`、`ai_cache.*`、`ai_client.ask`、`ai_client.load_cfg`
- Produces: `make_handler(db_path, cfg)` + `run(db_path, host, port=8092)`

**处理流程(POST /api/ask):**
1. 验 `Bearer token` → `auth.lookup_session`,None 则 401 `{error:'unauthorized'}`
2. 读 body `{question}`,非空且 ≤200 字,否则 400 `{error:'bad_question'}`
3. `compliance.input_blocked(question)` → True 则直接返 `{answer: SAFE_FALLBACK, cached: False, blocked: True}`(200,不调 AI)
4. `ai_cache.get_cached` 命中 → 返 `{answer, cached: True}`
5. 未命中 → `ai_client.ask`;异常则 502 `{error:'ai_unavailable'}`
6. `compliance.has_banned(answer)` 非空 → answer 替换为 SAFE_FALLBACK(不写缓存)
7. 干净答案 → `ai_cache.put_cached` + 返 `{answer, cached: False}`

- [ ] **Step 1: 写失败测试(用真 sqlite + mock ai_client.ask,起真实 handler 走 HTTP)**

```python
# 追加到 backend/tests/test_ai.py
import threading, json as _json, urllib.request as _req
from http.server import HTTPServer
import ai_server

class TestAiServer(unittest.TestCase):
    @classmethod
    def setUpClass(cls):
        cls.dbfile = ':memory:'  # 见下方 note:实际用临时文件
    def _start(self, db_path, cfg):
        handler = ai_server.make_handler(db_path, cfg)
        httpd = HTTPServer(('127.0.0.1', 0), handler)
        port = httpd.server_address[1]
        t = threading.Thread(target=httpd.serve_forever, daemon=True); t.start()
        return httpd, port

    def test_missing_token_returns_401(self):
        import tempfile, db as dbm, auth
        f = tempfile.NamedTemporaryFile(suffix='.db', delete=False); f.close()
        dbm.init_db(f.name)
        cfg = {"FR_AI_KEY":"k","FR_AI_BASE":"https://x","FR_AI_MODEL":"m"}
        httpd, port = self._start(f.name, cfg)
        req = _req.Request(f'http://127.0.0.1:{port}/api/ask',
                           data=b'{"question":"什么是ETF"}',
                           headers={'Content-Type':'application/json'})
        try:
            _req.urlopen(req)
            self.fail("should 401")
        except _req.HTTPError as e:
            self.assertEqual(e.code, 401)
        finally:
            httpd.shutdown()

    def test_valid_token_cache_miss_calls_ai_and_caches(self):
        import tempfile, db as dbm, auth
        f = tempfile.NamedTemporaryFile(suffix='.db', delete=False); f.close()
        dbm.init_db(f.name)
        conn = dbm.get_conn(f.name)
        conn.execute("INSERT INTO users (username,password_hash,salt,created_at) VALUES ('u','h','s','t')")
        conn.commit()
        uid = conn.execute("SELECT id FROM users").fetchone()[0]
        token = auth.create_session(conn, uid); conn.close()
        cfg = {"FR_AI_KEY":"k","FR_AI_BASE":"https://x","FR_AI_MODEL":"m"}
        with mock.patch('ai_client.ask', return_value="ETF是一种基金") as m:
            httpd, port = self._start(f.name, cfg)
            req = _req.Request(f'http://127.0.0.1:{port}/api/ask',
                               data='{"question":"什么是ETF"}'.encode(),
                               headers={'Content-Type':'application/json',
                                        'Authorization': 'Bearer '+token})
            body = _json.loads(_req.urlopen(req).read())
            self.assertEqual(body['answer'], "ETF是一种基金")
            self.assertFalse(body['cached'])
            # 第二次同问题命中缓存,不再调 AI
            req2 = _req.Request(f'http://127.0.0.1:{port}/api/ask',
                                data='{"question":"什么是 ETF"}'.encode(),
                                headers={'Content-Type':'application/json',
                                         'Authorization': 'Bearer '+token})
            body2 = _json.loads(_req.urlopen(req2).read())
            self.assertTrue(body2['cached'])
            self.assertEqual(m.call_count, 1)
            httpd.shutdown()

    def test_banned_answer_replaced_with_fallback(self):
        import tempfile, db as dbm, auth, compliance
        f = tempfile.NamedTemporaryFile(suffix='.db', delete=False); f.close()
        dbm.init_db(f.name)
        conn = dbm.get_conn(f.name)
        conn.execute("INSERT INTO users (username,password_hash,salt,created_at) VALUES ('u','h','s','t')")
        conn.commit(); uid = conn.execute("SELECT id FROM users").fetchone()[0]
        token = auth.create_session(conn, uid); conn.close()
        cfg = {"FR_AI_KEY":"k","FR_AI_BASE":"https://x","FR_AI_MODEL":"m"}
        with mock.patch('ai_client.ask', return_value="这只票必涨,建议买入"):
            httpd, port = self._start(f.name, cfg)
            req = _req.Request(f'http://127.0.0.1:{port}/api/ask',
                               data='{"question":"帮我看看"}'.encode(),
                               headers={'Content-Type':'application/json',
                                        'Authorization': 'Bearer '+token})
            body = _json.loads(_req.urlopen(req).read())
            self.assertEqual(body['answer'], compliance.SAFE_FALLBACK)
            httpd.shutdown()
```

Note: in-memory sqlite 不能跨线程共享,测试统一用 `tempfile` 临时 db 文件。

- [ ] **Step 2: 运行验证失败**

Run: `cd backend && python3 -m unittest tests.test_ai -v`
Expected: FAIL —— `No module named 'ai_server'`

- [ ] **Step 3: 写实现**

```python
# backend/ai_server.py
import json
from http.server import BaseHTTPRequestHandler, HTTPServer
import db as db_module
import auth, compliance, ai_cache, ai_client

def make_handler(db_path, cfg):
    class Handler(BaseHTTPRequestHandler):
        def log_message(self, *a): pass

        def _cors(self):
            self.send_header('Access-Control-Allow-Origin', '*')
            self.send_header('Access-Control-Allow-Headers', 'Content-Type, Authorization')
            self.send_header('Access-Control-Allow-Methods', 'POST, OPTIONS')

        def _send_json(self, status, obj):
            payload = json.dumps(obj).encode('utf-8')
            self.send_response(status); self._cors()
            self.send_header('Content-Type', 'application/json; charset=utf-8')
            self.send_header('Content-Length', str(len(payload)))
            self.end_headers(); self.wfile.write(payload)

        def _read_json(self):
            length = int(self.headers.get('Content-Length') or 0)
            if not length: return {}
            try: return json.loads(self.rfile.read(length) or b'{}')
            except json.JSONDecodeError: return None

        def _authed_uid(self):
            hdr = self.headers.get('Authorization') or ''
            if not hdr.startswith('Bearer '): return None
            conn = db_module.get_conn(db_path)
            try: return auth.lookup_session(conn, hdr[len('Bearer '):])
            finally: conn.close()

        def do_OPTIONS(self):
            self.send_response(204); self._cors(); self.end_headers()

        def do_POST(self):
            if self.path.split('?')[0] != '/api/ask':
                return self._send_json(404, {'error': 'not_found'})
            uid = self._authed_uid()
            if uid is None:
                return self._send_json(401, {'error': 'unauthorized'})
            data = self._read_json()
            if data is None:
                return self._send_json(400, {'error': 'bad_json'})
            question = (data.get('question') or '').strip()
            if not question or len(question) > 200:
                return self._send_json(400, {'error': 'bad_question'})
            if compliance.input_blocked(question):
                return self._send_json(200, {'answer': compliance.SAFE_FALLBACK,
                                             'cached': False, 'blocked': True})
            conn = db_module.get_conn(db_path)
            try:
                hit = ai_cache.get_cached(conn, question)
                if hit is not None:
                    return self._send_json(200, {'answer': hit, 'cached': True})
                try:
                    answer = ai_client.ask(question, cfg)
                except Exception:
                    return self._send_json(502, {'error': 'ai_unavailable'})
                if compliance.has_banned(answer):
                    return self._send_json(200, {'answer': compliance.SAFE_FALLBACK,
                                                 'cached': False})
                ai_cache.put_cached(conn, question, answer)
                return self._send_json(200, {'answer': answer, 'cached': False})
            finally:
                conn.close()

    return Handler

def run(db_path=db_module.DEFAULT_DB_PATH, host='0.0.0.0', port=8092):
    db_module.init_db(db_path)
    conn = db_module.get_conn(db_path)
    ai_cache.ensure_table(conn); conn.close()
    cfg = ai_client.load_cfg()
    httpd = HTTPServer((host, port), make_handler(db_path, cfg))
    print(f'[finrookie-ai] listening on {host}:{port}')
    httpd.serve_forever()

if __name__ == '__main__':
    run()
```

- [ ] **Step 4: 运行验证通过**

Run: `cd backend && python3 -m unittest tests.test_ai -v`
Expected: PASS(累计 14 条,含 401/缓存命中/违规替换)

- [ ] **Step 5: 提交**

```bash
git add finrookie/backend/ai_server.py finrookie/backend/tests/test_ai.py
git commit -m "feat(backend): add standalone AI ask server on 8092 with token auth + cache + compliance"
```

---

### Task 5: 前端 AI 调用模块 js/ai.js

**Files:**
- Create: `js/ai.js`
- Test: `js/ai.test.mjs`(node 断言,mock fetch)

**Interfaces:**
- Consumes: `auth.js` 的 token getter(现有 `js/auth.js`,读其导出的 token 存取方式)
- Produces: `askAI(question, token) -> Promise<{answer, cached}>`,网络/HTTP 错误抛带 code 的 Error

- [ ] **Step 1: 先读现有 auth.js 确认 token 如何取**

Run: `cat js/auth.js`
目的:确认 token 的读取导出名(如 `getToken()` 或某模块变量),Step 3 里 askAI 的 token 来源要对齐。**若 auth.js 已有 getToken 则 ai.js 直接 import 复用,否则 askAI 接受 token 参数由 app.js 传入**(本计划默认后者:`askAI(question, token)`)。

- [ ] **Step 2: 写失败测试**

```javascript
// js/ai.test.mjs
import assert from 'node:assert';
import { askAI } from './ai.js';

globalThis.fetch = async (url, opts) => ({
  ok: true, status: 200,
  json: async () => ({ answer: 'ETF是一种基金', cached: false }),
});
const r = await askAI('什么是ETF', 'faketoken');
assert.equal(r.answer, 'ETF是一种基金');
assert.equal(r.cached, false);

// HTTP 错误应抛
globalThis.fetch = async () => ({ ok: false, status: 401, json: async () => ({error:'unauthorized'}) });
let threw = false;
try { await askAI('x', 't'); } catch (e) { threw = true; assert.equal(e.code, 'HTTP_401'); }
assert.ok(threw, 'should throw on 401');
console.log('ai.test.mjs OK');
```

- [ ] **Step 3: 运行验证失败**

Run: `node js/ai.test.mjs`
Expected: FAIL —— 找不到 `./ai.js`

- [ ] **Step 4: 写实现**

```javascript
// js/ai.js
const AI_BASE = 'http://10.159.3.80:8092';
const TIMEOUT_MS = 22000;

export async function askAI(question, token) {
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), TIMEOUT_MS);
  try {
    const res = await fetch(`${AI_BASE}/api/ask`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${token}` },
      body: JSON.stringify({ question }),
      signal: controller.signal,
    });
    if (!res.ok) {
      const err = new Error(`HTTP ${res.status}`);
      err.code = `HTTP_${res.status}`;
      throw err;
    }
    return await res.json();
  } catch (e) {
    if (e.name === 'AbortError') { e.code = 'TIMEOUT'; }
    e.code = e.code || 'FETCH_FAILED';
    throw e;
  } finally {
    clearTimeout(timer);
  }
}
```

- [ ] **Step 5: 运行验证通过**

Run: `node js/ai.test.mjs`
Expected: PASS —— 打印 `ai.test.mjs OK`

- [ ] **Step 6: 提交**

```bash
git add finrookie/js/ai.js finrookie/js/ai.test.mjs
git commit -m "feat(frontend): add ai.js module for AI ask with timeout and error codes"
```

---

### Task 6: app.js 接入 AI 兜底状态与方法

**Files:**
- Modify: `js/app.js`(术语搜索相关区域:`search / searchResults / searchMode / exitSearch` 附近)

**Interfaces:**
- Consumes: `askAI` from `./ai.js`;现有登录态(app.js 里 `isLoggedIn` / token 存取,实施时读现状对齐命名);现有 `searchMode` / `searchResults`
- Produces:
  - 状态 `aiAnswer(null) / aiAsking(false) / aiError(null) / aiCached(false)`
  - 方法 `askAI()`(组件方法,内部调 ai.js 的 askAI)
  - getter `canAskAI`(`searchMode && searchResults.length === 0 && isLoggedIn`)
  - `exitSearch()` / `selectSearchResult()` 里重置 AI 三态

- [ ] **Step 1: 读 app.js 现状确认命名**

Run: `grep -n "searchMode\|searchResults\|exitSearch\|isLoggedIn\|token" js/app.js`
目的:确认登录态 getter 名、token 来源、exitSearch 位置。下面代码里的 `this.isLoggedIn` / `this.authToken` 按实际命名替换。

- [ ] **Step 2: 顶部 import 加入 askAI**

在 app.js 现有 import 区加:
```javascript
import { askAI as askAIApi } from './ai.js';
```

- [ ] **Step 3: Alpine data 加 AI 状态**

在组件返回对象里(search 相关状态旁)加:
```javascript
aiAnswer: null,
aiAsking: false,
aiError: null,
aiCached: false,
get canAskAI() {
  return this.searchMode && this.searchResults.length === 0 && this.isLoggedIn;
},
```

- [ ] **Step 4: 加 askAI 方法**

```javascript
async askAI() {
  if (!this.isLoggedIn || !this.activeSearchQueryText()) return;
  this.aiAnswer = null; this.aiError = null; this.aiAsking = true;
  try {
    const r = await askAIApi(this.searchQuery.trim(), this.authToken);
    this.aiAnswer = r.answer;
    this.aiCached = !!r.cached;
    this.track('ai_ask', { cached: !!r.cached });
  } catch (e) {
    this.aiError = (e.code === 'TIMEOUT')
      ? '网络不太顺,待会再试试~'
      : '暂时问不了,待会再试试~';
  } finally {
    this.aiAsking = false;
  }
}
```
Note: `activeSearchQueryText()` 若 app.js 无此辅助,直接用 `this.searchQuery.trim()` 判空;`this.track` 是现有埋点方法(确认名字)。

- [ ] **Step 5: exitSearch/selectSearchResult 重置 AI 态**

在现有 `exitSearch()` 与 `selectSearchResult()` 内追加:
```javascript
this.aiAnswer = null; this.aiError = null; this.aiAsking = false; this.aiCached = false;
```

- [ ] **Step 6: 语法检查**

Run: `node --check js/app.js`
Expected: 无输出(语法通过)

- [ ] **Step 7: 提交**

```bash
git add finrookie/js/app.js
git commit -m "feat(frontend): wire AI fallback state and askAI method into search flow"
```

---

### Task 7: index.html 术语搜索空态加 AI 兜底 UI

**Files:**
- Modify: `index.html`(术语搜索空态区块)

**Interfaces:**
- Consumes: `canAskAI` / `askAI()` / `aiAnswer` / `aiAsking` / `aiError` / `aiCached`(Task 6 产出)

- [ ] **Step 1: 定位空态区块**

Run: `grep -n "没找到\|searchResults.length === 0\|searchMode" index.html`
目的:找到术语搜索"没找到"空态所在的元素。

- [ ] **Step 2: 在空态下方插入 AI 兜底块**

在"没找到"空态元素内部/下方插入(用现有卡片视觉类名,实施时对齐 index.html 既有 class):
```html
<!-- AI 兜底:仅已登录且本地未命中时 -->
<div x-show="canAskAI" class="mt-3">
  <button @click="askAI()" x-show="!aiAnswer && !aiAsking"
          class="w-full rounded-2xl bg-brand/10 text-brand py-3 font-medium">
    🤖 没找到?问问 AI
  </button>
  <div x-show="aiAsking" class="py-4 text-center text-ink-soft">正在问 AI…</div>
  <div x-show="aiError" x-text="aiError" class="py-3 text-center text-clay"></div>
  <!-- 回答卡:所有 aiAnswer.xxx 必须 aiAnswer && 守卫;此处仅渲染文本用 x-text 安全 -->
  <article x-show="aiAnswer" class="rounded-3xl bg-paper p-4 shadow">
    <p class="text-ink leading-relaxed" x-text="aiAnswer"></p>
    <p class="mt-3 text-xs text-ink-soft">AI 科普仅供学习,不构成投资建议</p>
  </article>
</div>
<!-- 未登录提示 -->
<div x-show="searchMode && searchResults.length === 0 && !isLoggedIn" class="mt-3 text-center text-ink-soft text-sm">
  登录后可以问 AI 哦
</div>
```
Note: 回答用 `x-text="aiAnswer"` 渲染纯文本(无裸读属性,天然安全);若改成读 `aiAnswer.xxx` 结构须加 `aiAnswer &&` 守卫。class 名(brand/ink/paper/clay)按 index.html 现有 tailwind token 对齐。

- [ ] **Step 3: 语法/标签配平检查**

Run: `grep -c "<article" index.html && grep -c "</article>" index.html`
Expected: 开合标签数量一致(与改动前相比 +1/+1)。

- [ ] **Step 4: 提交**

```bash
git add finrookie/index.html
git commit -m "feat(frontend): add AI fallback UI in glossary search empty state"
```

---

### Task 8: sw.js 版本升级 + 部署脚本

**Files:**
- Modify: `sw.js`(CACHE_VERSION + APP_SHELL 加 js/ai.js)

**Interfaces:**
- Consumes: 前面所有前端文件

- [ ] **Step 1: 读 sw.js 现状**

Run: `grep -n "CACHE_VERSION\|APP_SHELL\|ai.js\|app.js" sw.js`
目的:确认当前版本号(记忆为 v12)与壳缓存清单格式。

- [ ] **Step 2: bump 版本并加 ai.js**

- `CACHE_VERSION` v12 → v13(按实际当前值 +1)。
- APP_SHELL 数组里 `js/app.js` 旁加 `'./js/ai.js'`。

- [ ] **Step 3: 语法检查**

Run: `node --check sw.js`
Expected: 无输出

- [ ] **Step 4: 提交**

```bash
git add finrookie/sw.js
git commit -m "chore(frontend): bump SW cache to v13 and precache ai.js"
```

---

### Task 9: 部署到服务器 + 冒烟验证

**Files:** 无代码改动,纯运维。

**前置:** 全部单测 + node 断言绿。

- [ ] **Step 1: 本机跑全部后端测试**

Run: `cd backend && python3 -m unittest tests.test_ai tests.test_auth tests.test_api tests.test_db -v`
Expected: 全绿(新 test_ai + 原有回归)

- [ ] **Step 2: scp 增量传后端(绝不碰 data/)**

```bash
SSH="ssh -i ~/.ssh/finrookie_deploy -o BatchMode=yes wangrenyi@10.159.3.80"
SCP="scp -i ~/.ssh/finrookie_deploy -o BatchMode=yes"
$SCP backend/compliance.py backend/ai_cache.py backend/ai_client.py backend/ai_server.py \
    wangrenyi@10.159.3.80:~/finrookie-app/backend/
```

- [ ] **Step 3: scp 增量传前端**

```bash
$SCP js/ai.js js/app.js index.html sw.js \
    wangrenyi@10.159.3.80:~/finrookie-app/
# 注意:js/ 在子目录
$SCP js/ai.js js/app.js wangrenyi@10.159.3.80:~/finrookie-app/js/
$SCP index.html sw.js wangrenyi@10.159.3.80:~/finrookie-app/
```
Note: 分开传,`js/*` 进 `~/finrookie-app/js/`,`index.html`/`sw.js` 进 `~/finrookie-app/`。

- [ ] **Step 4: 服务器起 8092(先前台验证再后台)**

```bash
$SSH "cd ~/finrookie-app/backend && python3 -c 'import ai_client; ai_client.load_cfg(); print(\"cfg ok\")'"
$SSH "cd ~/finrookie-app/backend && (setsid python3 ai_server.py >~/fr-ai.log 2>&1 &)"
sleep 1
$SSH "tail ~/fr-ai.log"
```
Expected: 日志 `[finrookie-ai] listening on 0.0.0.0:8092`

- [ ] **Step 5: 冒烟 —— 无 token 应 401**

```bash
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://10.159.3.80:8092/api/ask \
  -H 'Content-Type: application/json' -d '{"question":"什么是ETF"}'
```
Expected: `401`

- [ ] **Step 6: 冒烟 —— 带真 token 走通(需先登录取 token)**

```bash
TOKEN=$(curl -s -X POST http://10.159.3.80:8091/api/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"<你的测试账号>","password":"<密码>"}' | python3 -c 'import sys,json;print(json.load(sys.stdin)["token"])')
curl -s -X POST http://10.159.3.80:8092/api/ask \
  -H 'Content-Type: application/json' -H "Authorization: Bearer $TOKEN" \
  -d '{"question":"什么是可转债"}'
```
Expected: 返 `{"answer":"...","cached":false}`;再跑一次同问题 `cached:true`。
Note: 测试账号密码由用户在会话里 `! curl ...` 自行执行,不写入代码/记忆。

- [ ] **Step 7: 加 crontab @reboot 自启**

```bash
$SSH "crontab -l | grep -q ai_server.py || (crontab -l; echo '@reboot cd ~/finrookie-app/backend && setsid python3 ai_server.py >~/fr-ai.log 2>&1') | crontab -"
$SSH "crontab -l"
```
Expected: 看到第 4 条 `@reboot ... ai_server.py`

- [ ] **Step 8: 真机验证清单(用户本机浏览器)**

无浏览器自动化,以下由用户在 `http://10.159.3.80:8090/` 手工确认(先清 SW / 硬刷新到 v13):
1. 未登录 → 搜不存在的词 → 显示"登录后可以问 AI 哦",无按钮。
2. 登录 → 搜不存在的词(如"雪球结构") → 出"🤖 问问 AI"按钮。
3. 点按钮 → loading → 出 AI 回答卡 + 免责声明。
4. 再搜同词点问 → 秒回(缓存)。
5. 搜命中本地词库的词(如"ETF") → 正常显示术语卡,**不出** AI 按钮。
6. 断网 → 点问 → 友好错误,本地搜索仍可用。

- [ ] **Step 9: 提交部署备忘(可选,更新 crontab 记录到文档)**

无代码则跳过;若更新运维文档则单独提交。

---

## Self-Review

**1. Spec coverage(逐节核对 2026-08-03-ai-qa-design.md):**
- §3 架构数据流 → Task 4(server 流程)+ Task 5/6/7(前端链路)✓
- §4.1 ai_server → Task 4 ✓;§4.2 ai_client → Task 3 ✓;§4.3 compliance → Task 1 ✓;§4.4 ai_cache 无 user_id → Task 2(含专门列检查测试)✓
- §5 隐私隔离(无列出接口 / token 只准入 / 缓存无 user_id) → Task 4 只实现 /api/ask 单接口 + Task 2 表结构测试 ✓
- §6.1 ai.js → Task 5 ✓;§6.2 app.js → Task 6 ✓;§6.3 index.html + 裸读守卫 → Task 7 ✓;§6.4 sw.js → Task 8 ✓
- §7 错误逐档降级 → Task 6 Step 4(TIMEOUT/其他)+ Task 7(未登录提示)✓
- §8 部署(scp 不碰 data / crontab 第4条 / curl 401 验证) → Task 9 ✓
- §9 测试(401/400/缓存命中/合规双拦/无 user_id 列) → Task 1-4 测试覆盖 ✓
- §10 非目标 → 计划未引入聊天页/限流/历史,符合 ✓

**2. Placeholder scan:** 无 TBD/TODO;错误处理均给出具体话术与 code 分支;测试均含真实断言代码。唯一"按实际命名对齐"处(app.js 的 isLoggedIn/authToken/track、index.html 的 tailwind class、sw.js 版本号)均配了 grep 定位步骤,非占位。

**3. Type consistency:**
- `askAI` 前端函数签名 `askAI(question, token)` 在 Task 5 定义、Task 6 调用一致(app.js 内用别名 `askAIApi` 避免与组件方法 `askAI()` 重名)✓
- `ai_cache` 函数名 `ensure_table/normalize/cache_key/get_cached/put_cached` 在 Task 2 定义、Task 4 调用一致 ✓
- `compliance` 的 `has_banned/input_blocked/SAFE_FALLBACK` 在 Task 1 定义、Task 4 调用一致 ✓
- `ai_client.ask(question, cfg)` / `load_cfg()` 在 Task 3 定义、Task 4 调用一致 ✓
- 后端返回形状 `{answer, cached}` 在 Task 4 产出、Task 5 测试、Task 6 消费一致 ✓
